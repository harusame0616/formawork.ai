{
	"dependencies": {
		"drizzle-orm": "catalog:",
		"postgres": "catalog:",
		"valibot": "catalog:"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"@workspace/tsconfig": "workspace:",
		"drizzle-kit": "catalog:",
		"tsx": "catalog:",
		"typescript": "catalog:"
	},
	"exports": {
		"./client": "./src/client.ts",
		"./schema/customer": "./src/schema/customer.ts",
		"./schema/customer-note": "./src/schema/customer-note.ts",
		"./schema/staff": "./src/schema/staff.ts"
	},
	"name": "@workspace/db",
	"private": true,
	"scripts": {
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate",
		"db:push": "drizzle-kit push",
		"db:reset": "pnpm --filter @repo/supabase supabase:reset && pnpm --filter @workspace/db db:push && pnpm --filter @workspace/db db:seed",
		"db:seed": "tsx --env-file-if-exists=.env src/seed.ts",
		"db:studio": "drizzle-kit studio",
		"type:check": "tsc --noEmit"
	},
	"type": "module",
	"version": "0.0.0"
}
